import SwiftUI
import NexusUI

public struct CommunicationView: View {
    public init() {}
    
    public var body: some View {
        Text("Email & Calendar Hub")
            .font(.title)
            .foregroundStyle(.secondary)
    }
}
